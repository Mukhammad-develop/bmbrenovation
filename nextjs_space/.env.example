# OpenAI API key used by the autoblog engine (scripts/autoblog/).
# Get one at https://platform.openai.com/api-keys
# For GitHub Actions, add this as a repo secret named OPENAI_API_KEY instead.
OPENAI_API_KEY=
